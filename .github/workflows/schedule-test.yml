name: Schedule Test

on:
  workflow_dispatch:

  schedule:
    - cron: '7,17,27,37,47,57 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test scheduled workflow
        run: echo "GitHub Actions schedule test"